extends layout.pug

block contents
    if errMsg
        span=errMsg
    form(method="POST") 
        input(name="title", type="text", required, placeholder="Title", maxlength="20")
        input(name="description", type="text", required, placeholder="Description", minlength="10")
        input(name="hashtags", type="text", required, placeholder="Hashtags : a, b, c")
        input(type="submit" value="upload Video") 
    

