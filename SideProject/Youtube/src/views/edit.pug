extends layout.pug

block contents 
    h1="Change Title of video"
    form(method="POST")
        input(name="title", placeholder="?" value=video.title, type="text", require)
        input(value="Save", type="submit") 
