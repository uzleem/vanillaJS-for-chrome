extends layout.pug

block contents 
    h1="Change Title of video"
    form(method="POST")
        input(name="title", placeholder="videoTitle", value=pageTitle, type="text", required)
        input(value="Save", type="submit") 
